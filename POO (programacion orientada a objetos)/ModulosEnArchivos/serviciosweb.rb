module ServicioWebAlmacenamiento
    def obtener(nombre_usuario, contraseña, ruta_servidor)
        puts "Obteniendo archivos..."
    end

    def guardar(nombre_usuario, contraseña, archivo, ruta_local, ruta_servidor)
        puts "Guardando archivo"
    end

    def eliminar(nombre_usuario, contraseña, ruta_servidor)
        puts "Eliminando archivo..."
    end
end